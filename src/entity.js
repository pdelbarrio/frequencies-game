class Entity {
  constructor(canvas, speed, color) {
    this.canvas = canvas;
    this.ctx = this.canvas.getContext("2d");
    this.size = 50;
    this.speed = speed;
    this.color = color;
  }

  
}
